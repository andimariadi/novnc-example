# Environment variables untuk noVNC proxy
NODE_ENV=production

# Port konfigurasi
HTTP_PORT=7575
WEBSOCKET_PORT=7576

# Default VNC target (bisa di-override via URL parameter)
DEFAULT_VNC_HOST=10.8.10.6
DEFAULT_VNC_PORT=5900

# Logging level (debug, info, warn, error)
LOG_LEVEL=info
